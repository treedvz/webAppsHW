import { DupeCheckDirective } from './dupe-check.directive';

describe('DupeCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new DupeCheckDirective();
    expect(directive).toBeTruthy();
  });
});
